/*C program that displays the first ten integers using a while loop*/
#include <stdio.h>

int main()
{
    int n = 1;
    while(n <= 10)
    {
        printf("Current value of n: %d\n", n);
        n++;
    }
    return 0;
}
